// TODO(덴): 이슈 테이블 체크박스 기능 구현 예정.(삭제 예감..)
const CheckBox = () => {
  return (
    <input
      type="checkbox"
      // disabled={disabled}
      // checked={checked}
      // onChange={({ target: { checked } }) => onChange(checked)}
    />
  );
};

export default CheckBox;
